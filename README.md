# Nature A Digital Hub
Run `powershell -ExecutionPolicy Bypass -File .\Start-DigitalHub.ps1`, then use http://localhost:8787/. Save changes writes data to browser localStorage; the current menu uses sessionStorage.
